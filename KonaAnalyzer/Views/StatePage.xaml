<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="KonaAnalyzer.Views.StatePage"
             xmlns:vm="clr-namespace:KonaAnalyzer.ViewModels"
             Title="{Binding Title}">
    
    <ContentPage.BindingContext>
        <vm:StateViewModel />
    </ContentPage.BindingContext>
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Primary">#2196F3</Color>
            <Color x:Key="Accent">#96d1ff</Color>
            <Color x:Key="LightTextColor">#999999</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

  
            <StackLayout Orientation="Vertical" Padding="16,40,16,40" Spacing="10">
                <Picker ItemsSource="{Binding Counties}" SelectedItem="{Binding County}"/>
                <Picker ItemsSource="{Binding Dates}" SelectedItem="{Binding Date}"/>
      
                <Label Text="Total Sick"/>
                <Label FontSize="22" Text="{Binding Current}"/>
                <Label Text="Change"/>
                <Label FontSize="12" Text="{Binding CurrentChange}" />
                <Label Text="Change Rate"/>
                <Label FontSize="12" Text="{Binding CurrentChangeRate}" />
                <BoxView HeightRequest="3" HorizontalOptions="FillAndExpand"/> 
                <Label Text="Total Deaths"/>
                <Label FontSize="22" Text="{Binding Dead}"/>
                <Label Text="Change"/>
                <Label FontSize="12" Text="{Binding DeadChange}" />
                <Label Text="Change Rate"/>
                <Label FontSize="12" Text="{Binding DeadChangeRate}" />

            </StackLayout> 
</ContentPage>
